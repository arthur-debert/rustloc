# Rust
/target/
**/*.rs.bk
# Cargo.lock IS tracked: rustloc ships a binary (`rustloc` CLI), and
# binary crates should commit Cargo.lock so users / CI / packagers
# build against the same dep graph that was tested.

# IDE
.idea/
*.iml
.vscode/
*.swp
*.swo
*~

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Padz
.padz

# Build artifacts
*.so
*.dylib
*.dll

# Debug
*.pdb

# Coverage
*.profraw
*.profdata
coverage/
lcov.info
