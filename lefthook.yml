# Generated by release-sync from arthur-debert/release@7a9c43db699f. Do not edit.
# Regenerate by running release-sync. Edit fragments in templates/components/<comp>/lefthook.fragment.yaml.

output:
  - meta
  - skips
  - summary
pre-commit:
  parallel: true
  commands:
    markdownlint:
      glob: "**/*.md"
      run: markdownlint {staged_files}
      priority: 2
    yamllint:
      glob: "**/*.{yml,yaml}"
      run: yamllint {staged_files}
      priority: 2
    shellcheck:
      glob: "**/*.{sh,bash}"
      run: shellcheck {staged_files}
      priority: 2
    cargo-fmt:
      glob: "**/*.rs"
      run: cargo fmt --all -- --check
      priority: 2
    cargo-clippy:
      glob:
        - "**/*.rs"
        - "**/Cargo.toml"
      run: cargo clippy --all-targets -- -D warnings
      priority: 2
